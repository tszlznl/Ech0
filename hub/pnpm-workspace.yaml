# pnpm 11.5 起，overrides / peerDependencyRules 不再从 package.json 的 "pnpm" 字段读取，
# 统一放在 pnpm-workspace.yaml。参见 https://pnpm.io/settings
overrides:
  'serialize-javascript@<7.0.5': ^7.0.5
peerDependencyRules:
  allowedVersions:
    'vite-plugin-pwa>vite': '8'
